# Created by https://www.toptal.com/developers/gitignore/api/node,python,react,flask,jetbrains,visualstudiocode
# Edit at https://www.toptal.com/developers/gitignore?templates=node,python,react,flask,jetbrains,visualstudiocode

### Python / Flask Specific (inside backend/) ###
# Virtual Environments
backend/venv/
backend/.venv/
backend/env/
backend/ENV/
backend/myenv/
backend/env.bak/
backend/venv.bak/

# Python cache files and byte-compiled / optimized / DLL files
backend/__pycache__/
backend/*.py[cod]
backend/*$py.class
backend/*.so

# Distribution / packaging (залишимо основні)
backend/build/
backend/dist/
backend/eggs/
backend/.eggs/
backend/wheels/
backend/*.egg-info/
backend/*.egg

# Installer logs
backend/pip-log.txt
backend/pip-delete-this-directory.txt

# Unit test / coverage reports (якщо плануєте тести)
backend/htmlcov/
backend/.coverage
backend/.coverage.*
backend/.pytest_cache/
backend/cover/

# Flask specific
backend/instance/*
!backend/instance/.gitignore # Дозволяє мати .gitignore всередині instance, якщо потрібно
backend/.webassets-cache

# Environments
backend/.env
backend/.env.*
backend/env.bak 
# backend/.envrc - залиште, якщо використовуєте direnv

### Node / React Specific (inside frontend/) ###
# Logs
frontend/logs
frontend/npm-debug.log*
frontend/yarn-debug.log*
frontend/yarn-error.log*
# frontend/lerna-debug.log* # Якщо не використовуєте Lerna
frontend/.pnpm-debug.log* # Якщо не використовуєте pnpm

# Runtime data
frontend/pids
frontend/*.pid
frontend/*.seed
frontend/*.pid.lock

# Coverage directory used by tools like istanbul
frontend/coverage
frontend/*.lcov

# nyc test coverage
frontend/.nyc_output

# Dependency directories - НАЙВАЖЛИВІШЕ ДЛЯ ФРОНТЕНДУ
frontend/node_modules/
# frontend/jspm_packages/ # Якщо не використовуєте jspm

# TypeScript cache (якщо будете використовувати TS)
frontend/*.tsbuildinfo

# Optional npm cache directory
frontend/.npm

# Optional eslint cache
frontend/.eslintcache

# Output of 'npm pack'
frontend/*.tgz

# dotenv environment variable files (для Node/React)
frontend/.env.development.local
frontend/.env.test.local
frontend/.env.production.local
frontend/.env.local

# parcel-bundler cache (якщо не використовуєте Parcel)
# frontend/.parcel-cache

# Next.js build output (якщо не використовуєте Next.js)
# frontend/.next
# frontend/out

# Create React App build directory
frontend/build/ # Важливо для React

# yarn v2 (якщо використовуєте Yarn 2+)
# frontend/.yarn/cache
# frontend/.yarn/unplugged
# frontend/.yarn/build-state.yml
# frontend/.yarn/install-state.gz
frontend/.pnp.* # .pnp вже є вище, але це ширше

### JetBrains IDEs (PyCharm, WebStorm, etc.) ###
# User-specific stuff
.idea/**/workspace.xml
.idea/**/tasks.xml
.idea/**/usage.statistics.xml
.idea/**/dictionaries/ # Додав слеш в кінці
.idea/**/shelf/ # Додав слеш в кінці

# Sensitive or high-churn files
.idea/**/dataSources/ # Додав слеш в кінці
.idea/**/dataSources.ids/ # Додав слеш в кінці
.idea/**/dataSources.local.xml
.idea/**/sqlDataSources.xml
.idea/**/dynamic.xml
.idea/**/uiDesigner.xml
.idea/**/dbnavigator.xml

# Gradle (якщо не використовуєте з JetBrains)
# .idea/**/gradle.xml
# .idea/**/libraries/

# CMake
cmake-build-*/

# IntelliJ (загальне)
# out/ # Може конфліктувати з Next.js 'out' або Python 'build', краще вказувати конкретніше для кожного проекту

### VisualStudioCode ###
# Застосовується до всього репозиторію, якщо .vscode в корені,
# або можна додати frontend/.vscode/* та backend/.vscode/* якщо налаштування специфічні
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Local History for Visual Studio Code (застосовується до всього репозиторію)
.history/

# Built Visual Studio Code Extensions
*.vsix

### General (застосовується до всього репозиторію) ###
# macOS
.DS_Store
**/.DS_Store # Щоб ловити .DS_Store у підпапках

# Backup files
*.backup.*
*.back.*
*.bak

# Temporary files
*.tmp
*~

# Log files (загальне)
*.log
